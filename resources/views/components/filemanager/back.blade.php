@imagemanager([
    'collection'        => config('administrable.media.collections.back.label'),
    'model'             =>  $model,
    'label'             =>  $label,
    'type'              =>  $type ?? null
])
