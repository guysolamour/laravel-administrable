@imagemanager([
    'collection'        => config('administrable.media.collections.images.label'),
    'model'             =>  $model,
    'label'             =>  $label,
    'type'              =>  $type ?? null
])
