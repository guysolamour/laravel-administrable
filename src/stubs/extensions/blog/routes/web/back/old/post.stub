<?php
use Illuminate\Support\Facades\Route;

Route::middleware(['{{singularSlug}}.auth'])->group(function () {
    Route::namespace('{{backNamespace}}')->group(function () {
      Route::prefix('{{extensionsFolder}}/{{extensionSingularSlug}}')->group(function(){
        Route::name('{{backLowerNamespace}}.{{extensionsFolder}}.{{extensionSingularSlug}}.post')->group(function () {
          Route::resource('posts','{{extensionsFolderClass}}\{{extensionClass}}\PostController')->names([
                'index'      => 'index',
                'show'       => 'show',
                'create'     => 'create',
                'store'      => 'store',
                'edit'       => 'edit',
                'update'     => 'update',
                'destroy'    => 'destroy',
          ]);
        });
      });
    });
});


