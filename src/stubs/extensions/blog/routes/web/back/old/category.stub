<?php
use Illuminate\Support\Facades\Route;

Route::middleware(['{{singularSlug}}.auth'])->group(function () {
    Route::namespace('{{backNamespace}}')->group(function () {
      Route::prefix('{{extensionsFolder}}/{{extensionSingularSlug}}')->group(function(){
        Route::name('{{backLowerNamespace}}.{{extensionsFolder}}.{{extensionSingularSlug}}.category')->group(function () {
          Route::resource('categories','{{extensionsFolderClass}}\{{extensionClass}}\CategoryController')->names([
                'index'      => 'index',
                'show'       => 'show',
                'create'     => 'create',
                'store'      => 'store',
                'edit'       => 'edit',
                'update'     => 'update',
                'destroy'    => 'destroy',
          ]);
        });
      });
    });
});

