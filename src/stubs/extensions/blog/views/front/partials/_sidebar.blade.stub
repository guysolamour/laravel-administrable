@php
    $recent_posts = \{{namespace}}\{{modelsFolder}}\{{extensionsFolderClass}}\{{extensionClass}}\Post::with('media')->online()->last(5)->get();
    $categories = \{{namespace}}\{{modelsFolder}}\{{extensionsFolderClass}}\{{extensionClass}}\Category::withCount(['posts' => fn($query) => $query->where('online', true)])->last()->get();
    $tags = \{{namespace}}\{{modelsFolder}}\{{extensionsFolderClass}}\{{extensionClass}}\Tag::withCount(['posts' => fn($query) => $query->where('online', true)])->last()->get();
@endphp

<div class="card mb-4" style="width: 18rem;">
    <div class="card-header">
        Recherche
    </div>
    <form action="{{ route('{{frontLowerNamespace}}.{{extensionsFolder}}.{{extensionSingularSlug}}.search') }}" method="get" class="p-2">
        <div class="form-group">
            <input type="search" name="q" id="q" class="form-control" required>
        </div>
        <button type="submit" class="btn  btn-secondary btn-sm btn-block"><i class="fas fa-search"></i> Recherche</button>
    </form>
</div>


<div class="card mb-4" style="width: 18rem;">
    <div class="card-header">
        Liste des categories
    </div>
    <ul class="list-group list-group-flush">
        @foreach ($categories as $category)
        <li class="list-group-item"><a href="{{ route('{{frontLowerNamespace}}.{{extensionsFolder}}.{{extensionSingularSlug}}.category', $category) }}">{{ $category->name }} ({{ $category->posts_count }})</a></li>
        @endforeach
    </ul>
</div>



<div class="card mb-4" style="width: 18rem;">
    <div class="card-header">
        Liste des étiquettes
    </div>
    <ul class="list-group list-group-flush">
        @foreach ($tags as $tag)
        <li class="list-group-item"><a href="{{ route('{{frontLowerNamespace}}.{{extensionsFolder}}.{{extensionSingularSlug}}.tag', $tag) }}">{{ $tag->name }} ({{ $tag->posts_count }})</a></li>
        @endforeach
    </ul>
</div>

<div class="card mb-4" style="width: 18rem;">
    <div class="card-header">
        Articles récents
    </div>
    <ul class="list-group list-group-flush">
        @foreach ($recent_posts as $post)
        <li class="list-group-item"><a href="{{ route('{{frontLowerNamespace}}.{{extensionsFolder}}.{{extensionSingularSlug}}.show', $post) }}">{{ $post->title }}</a></li>
        @endforeach
    </ul>
</div>
