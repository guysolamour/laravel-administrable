              <div class="nav-item">
                    <a href="{{ route('{{backLowerNamespace}}.mailbox.index') }}">
                        <i class="fa fa-envelope"></i><span>Messagerie</span>
                        @php
                            $new = \{{namespace}}\{{modelsFolder}}\Mailbox::unread()->count()
                        @endphp
                        @if($new)
                        <span class="badge badge-success">New {{ $new }}</span>
                        @endif
                    </a>
                </div>
