<?php

Route::middleware(['{{singularSlug}}.auth'])->group(function () {
    Route::namespace('{{backNamespace}}')->group(function () {

         Route::post('model/destroy/all', 'PageController@destroyAll')->name('{{backLowerNamespace}}.model.destroy.all');

    });
});
