 <?php
use Spatie\Honeypot\ProtectAgainstSpam;

Route::namespace(config('administrable.front_namespace'))->group(function () {

    /*Contact */
    Route::get('/contact', 'ContactController@create')->name('user.contact.create');
    Route::post('/contact', 'ContactController@store')->name('user.contact.store')->middleware(ProtectAgainstSpam::class);

});
